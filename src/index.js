import React from "react";
import ReactDOM from "react-dom";

import Parchateconlasflores from "../src/Parchateconlasflores";

const rootElement = document.getElementById("root");
ReactDOM.render(<Parchateconlasflores />, rootElement);
